# v0.1.1 Backlog

Post-release improvements discovered during Phase 4C-K closeout.
Do NOT include scanner feature changes unless independently verified.

## P0 — High Priority

None currently identified.

## P1 — Medium Priority

1. **Glama listing refresh** — Glama has indexed the old repo name (`haiec-ai-agent-security-free-mcp`). Submit the new `ai-appsec` identity to Glama for re-indexing, or wait for auto-ingestion from the Official MCP Registry to propagate.
2. **Smithery listing** — Submit to https://smithery.ai/new for one-click install discovery. Free, requires GitHub URL.
3. **awesome-mcp-servers PR** — Submit a PR to the community-curated GitHub list for additional discovery.
4. **MCP client configuration examples** — Add Cursor, Claude Desktop, and Windsurf MCP configuration examples to README.

## P2 — Low Priority

5. **npm discovery keywords** — Current keywords are comprehensive. Monitor npm search ranking for "ai security", "ai agent security", "mcp security" queries.
6. **README installation video** — Consider a short GIF or video showing `ai-appsec doctor` + `ai-appsec setup` + first scan.
7. **Release automation** — Consider combining qualification + publish into a single dispatch workflow for future releases.
8. **Test runtime optimization** — Output-contract tests take ~520s due to 14 sequential Semgrep scans on Windows. Consider parallelizing or caching.
